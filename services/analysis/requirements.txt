# Analysis service dependencies
# Shared deps (pydantic, supabase, etc.) come from the repo-root requirements.txt / cntr-shared package.
# The similarity engine itself uses only the Python standard library (no NLP/ML deps).
